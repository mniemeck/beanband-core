/**
 * The <em>Engineer</em> transforms the MIDI Model into a Sequence ready to be
 * rendered. It assembles the bits of the Music Model created from the various
 * Musicians into a single MIDI Sequence that contains the aggregated
 * information MIDI events together with all additional data required by the
 * MIDI standard.
 * 
 * @author Michael Niemeck
 */
package org.beanband.engineer;